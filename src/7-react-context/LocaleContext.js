import React from "react";

// const LocaleContext = React.createContext();

// set default value if no parent Providers are provided in the parent component for the Consummers
const LocaleContext = React.createContext("defaultValue hehe");
console.log(LocaleContext);

export default LocaleContext;